# Звуки

Звуки генерируются через Web Audio API в `js/audio.js` (без MP3).

Чтобы добавить файлы:
1. Положите `engine.mp3`, `honk.mp3`, `music.mp3` сюда
2. В `audio.js` добавьте: `new Audio('sounds/engine.mp3').play()`
